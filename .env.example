# Environment Variables — Family Salon

# Server port (defaults to 5000 if not set)
PORT=5000

# Optional: PostgreSQL connection string for persistent contact form storage.
# If not set, the app uses an in-memory store (data resets on restart — great for demos!).
# DATABASE_URL=postgresql://user:password@host:5432/dbname
